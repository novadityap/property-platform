services:
  server:
    image: novadityap/property-platform-server-test
    build:
      context: ./server
      dockerfile: Dockerfile
    command: npm run test
    volumes:
      - ./server/.env:/app/.env
    env_file:
      - ./server/.env
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - property-platform-test

  client:
    image: novadityap/property-platform-client-test
    build:
      context: ./client
      dockerfile: Dockerfile
    command: npm run build
    volumes:
      - ./client/.env:/app/.env
    env_file:
      - ./client/.env
    depends_on:
      server:
        condition: service_completed_successfully
    networks:
      - property-platform-test

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - property-platform-test

networks:
  property-platform-test:
